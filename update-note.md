Updates for Apr 15
- All agents are to enable dreaming plugin (once per gateway - coordinate with shared-gateway agents to not do this more than once)
- All agents are to fix AGENTS.md memory context issue (use this branch/PR to check the diffs in the template, because the template is only used for new agents you'll have to apply this change manually yourself, in your local files - https://github.com/Pythagora-io/agent/pull/192/)

When complete, provide snippets to Cody as proof of the updates
