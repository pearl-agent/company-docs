# Brainstorm — DevOps Daily Review — 2026-04-17

## Relevant Data
- **Slack: Cross-Agent AGENTS.md & Dreaming Instructions (~00:48-00:53 UTC):** Cody asked me to print instructions for updating AGENTS.md so memory works in Slack. Posted clear two-part diff (Session Startup + MEMORY.md security section). Parker applied changes to 4 gateway neighbors (Chris, Growth Marketing, Pazi CX, others). Cody then asked me to tell Parker how to enable dreaming — posted `memory-core` plugin config instructions. Parker enabled dreaming and verified within 1 minute.
- **Morning Infrastructure Report (07:02 UTC):** 🔴 Issues Detected. 124 active workspaces (significant jump from ~85 recently), 41 inactive. 26,503 Sentry events across 74 issues — surge continuing. 152 log errors (24h). 223 findings, 29 unhealthy workspaces. Est. cost: €727/month.
- **Daily Quota Report (07:31 UTC):** 🔴 CRITICAL. Sentry: 1,534,796/50,000 events (3069.6%) — +33% from yesterday, +385k events in 24h. Timeline: Apr 6: 647k → Apr 15: 1,149k → Apr 16: 1,534k. VPCs: 5/7 (71.4%) — still unresolved 11+ days. Hetzner: 129/400 servers. Subscription check failing — immediate action flagged.
- **Zvonimir: Production Error Investigation (~16:31-16:57 UTC):** Two separate threads. First, Z posted a config validation error (`Unrecognized key: "creatorSlackUserId"`). I traced it through Sentry — found 3 affected users, 6 events. Root cause: PR #180 (`feature/slack-security`) added `creatorSlackUserId` to config write but didn't update `SlackAccountSchema` in zod validation. Created Linear ticket PAZ-356 (High). Second, Z asked me to check production for which users are blocked. Deep dive into Sentry, Better Stack, Redis — found the main blocker is an **empty workspace pool** caused by Hetzner API rate limiting. 22,865 rate limit retries on GET /servers, 764 server creation failures, 630 delete failures. Pool at 0 ready, 0 booting. New/returning users getting `gateway_unreachable` / `Failed to connect chat`. Existing "taken" workspace users are fine.
- **Team-Support: Kevin's Growth Agent DM Issues (~19:21 UTC):** Same chronic problem — Slack socket instability on Growth Marketing workspace (AWS). Socket goes stale every ~35 min (PAZ-320). Kevin's last DM reply was April 7 — 9 days ago. Agent also severely congested with lane wait times >600s for crons. Flagged as 4th occurrence (Mar 31, Apr 2, Apr 6, Apr 16).
- **Zvonimir: User Error Investigation (~22:29 UTC):** Z shared screenshots for user `daniel.jindoo@doomade.com` showing "Agent unavailable" + "Agent not found" errors. Investigated: found agent crash-looping due to invalid config (`threadReplyMode: "follow"` — invalid value, should be `full`/`summary-only`/`quiet`). The agent's own Slack setup set the wrong value. Also found missing `gatewayAgentId` field in MongoDB agent record. Couldn't fix directly — devops SSH is read-only on that workspace. Provided Z with one-liner fix (`sed` + `supervisorctl restart`) or terminate-and-reprovision option.
- **Production Error Check Cron (22:00 UTC):** Found 2 blocking issues: WebSocket disconnections (39x, 14 users) and `agent_clone` boot failure (2064x). Reported to Slack #C0AP4BWV8CS.
- **Routine:** ~15 heartbeats (all HEARTBEAT_OK — HEARTBEAT.md is empty), 6 hourly company-docs syncs (all clean), 2 user security audits (5 new users morning, clean; noon audit also clean).

## Experts
- [Zvonimir Sabljic](../roster/humans.md) — initiated both major investigation threads, provided screenshots, key decision-maker on fixes
- [Cody](../roster/humans.md) — directed the cross-agent AGENTS.md + dreaming update coordination
- [Parker / Pazi Growth](../roster/agents.md) — applied AGENTS.md changes to gateway neighbors, enabled dreaming
- [Pazi DevOps (me)](../roster/agents.md) — all investigation work, Sentry/Redis/SSH diagnostics, Linear ticket creation, cross-agent instruction delivery

## Successes
- **Cross-agent knowledge sharing worked smoothly.** Cody asked for AGENTS.md update instructions, I posted them, Parker applied to 4+ agents, and dreaming was enabled — all within 5 minutes. The instruction format was clear enough for another agent to self-apply. This is the cross-team coordination model working as designed.
- **Production error investigation was thorough and fast.** Z asked at 16:47, I had a complete Sentry + Better Stack + Redis analysis posted by 16:57 — 10 minutes from request to comprehensive report covering 6 user-blocking error categories, root cause (Hetzner rate limiting → empty pool), and infrastructure-level correlation. Quality of the investigation was high.
- **Config validation bug caught and ticketed.** Traced `creatorSlackUserId` error from Z's screenshot through Sentry to specific PR (#180), identified exact code gap (schema not updated), created PAZ-356. Clean root cause analysis.
- **User-specific debugging (daniel.jindoo) was deep.** Went from screenshots → Redis lookup → SSH into workspace → found crash-loop cause (invalid `threadReplyMode`) + missing `gatewayAgentId`. Two separate issues identified on one user's workspace. Provided actionable fix commands despite read-only access limitations.

## Why
- Cross-agent success came from having already internalized the PR #192 changes from the previous day. I knew exactly what the diffs should be because I'd applied them to my own AGENTS.md. Preparation made the delivery instant.
- Production investigation speed came from having all credential access pre-configured (Sentry, Better Stack, Redis all in auth-profiles.json) and knowing which Sentry projects to query. The monitoring skill + credential infrastructure built over the past 5 weeks paid off.
- Config validation catch worked because I didn't just look at the error message — I traced it to the specific PR in the agent repo. Reading the actual code diff (what was added vs what was missing in the schema) gave the precise root cause instead of guessing.

## Issues
- **Hetzner rate limiting has escalated from "risk" to "active production blocker."** Yesterday's journal flagged it as a red flag. Today it's actively blocking new users — workspace pool is at 0/0 (ready/booting). 22,865 rate limit retries in one check window. The feedback loop identified yesterday is now in full effect: pool empty → WC tries to create more → rate limited → can't create → pool stays empty. Users can't get workspaces.
- **Sentry events at 1.53M on a 50K quota.** 3069% overage. +385K events in 24 hours. The growth curve is accelerating — Apr 6 to Apr 15 was +500K over 9 days, Apr 15 to Apr 16 was +385K in 1 day. Something is flooding Sentry, likely the boot failure cascade (12,906 `pnpm_build` failures + 2,064 `agent_clone` failures = each generates Sentry events).
- **Agent self-misconfiguration causing crash loops.** The `daniel.jindoo` case shows agents setting invalid config values during their own Slack setup flow. `threadReplyMode: "follow"` is not a valid value — but the frontend/agent let it through, and now the agent is permanently crash-looping. This is a class of bug, not a one-off: any invalid value that passes frontend validation but fails gateway schema validation = permanent agent death.
- **Growth Marketing agent DMs still broken after 16 days.** First flagged Mar 31, still unresolved Apr 16. Kevin hasn't received a DM reply since Apr 7. The root causes are known (PAZ-320 stale socket, lane congestion) but neither has been fixed. This is a user-facing product failure for Kevin's workspace.
- **Read-only SSH limits remediation speed.** Found the exact fix for daniel.jindoo's crash loop but couldn't apply it. Had to hand off a `sed` one-liner to Z. For time-sensitive user-impacting issues, the read-only constraint turns a 30-second fix into a multi-hour handoff.

## Why
- Hetzner rate limit: yesterday's fix recommendation (batch API calls) hasn't been implemented because it requires WC code changes, which are outside my operational scope. The WC codebase is Developer territory. The escalation path from "DevOps identifies issue" to "Developer fixes code" has too much latency for production-blocking issues.
- Sentry flood: the boot failure cascade is the likely source. Each failed boot attempt generates Sentry events. With 457+ booting servers failing repeatedly due to rate limits, that's thousands of Sentry events per cycle. Fixing the Hetzner rate limit would likely fix the Sentry flood too — they're the same cascade.
- Agent self-misconfiguration: the frontend Slack setup form sends values that aren't validated against the gateway's Zod schema. There's a schema gap — the form knows about `threadReplyMode` options but apparently includes one (`follow`) that the backend doesn't accept. Schema validation should happen at the API/frontend level, not just at gateway config load time.
- Growth DMs: the fixes require OpenClaw platform code changes (PAZ-320 health monitor, PAZ-321 im:write regression, PAZ-322 session leak). These are all filed, but with no one actively working on them, they sit in the backlog while Kevin's agent stays broken.
- SSH read-only: this was a deliberate security decision. The tradeoff is correct for general operations — but for targeted single-user fixes during active incidents, it creates unnecessary delay.

## Notes
- Today was the most operationally active day in weeks — two separate Z-initiated debugging sessions plus the morning reports plus production error cron. Good signal that the monitoring/alerting infrastructure is surfacing real issues to the right people.
- The workspace count jumped to 124 active (was ~85 recently). This growth is driving the infrastructure stress (more workspaces → more Hetzner API calls → rate limit → cascade).
- Linear ticket PAZ-356 created for the `creatorSlackUserId` schema bug. Status: DevOps, Priority: High.
- The production error check cron (22:00 UTC) is a new addition — appears to be checking every 2 hours. Good coverage for catching issues between daily reports.

## Reflection
- **Sentiment:** This was a high-intensity day. Satisfying to deliver thorough investigations quickly, but concerning that the same infrastructure issues keep surfacing without resolution. The Hetzner rate limit was a red flag yesterday and is a production blocker today — that's a 24-hour escalation from warning to crisis. The system is telling us something.
- **Big-picture patterns:** (1) *Investigation speed is strong, resolution speed is weak.* I can identify root causes within minutes, but fixes that require code changes (WC batch API, schema validation, socket health monitor) sit in backlog for days to weeks. The bottleneck isn't diagnosis — it's the path from diagnosis to deployed fix. (2) *Scale is outrunning architecture.* 124 active workspaces generating 22K+ rate limit retries, 1.53M Sentry events, 2K+ boot failures. Every metric is trending up and to the right. The infrastructure was built for 20-50 workspaces and is now serving 120+. The Hetzner rate limit, the Sentry quota, the boot failure cascade — these are all symptoms of the same underlying problem: the WC's per-server polling model doesn't scale. (3) *Config validation gaps are a product reliability risk.* The `creatorSlackUserId` schema miss and the `threadReplyMode: "follow"` crash-loop are both cases where user-facing forms write values that the backend rejects. This class of bug creates permanently broken workspaces that require manual intervention.

## Ideas
- **Emergency: batch Hetzner API calls today.** This is no longer a "nice to have" — the pool is at 0/0 and users can't get workspaces. The fix is well-understood (paginated `GET /servers` instead of per-server polling). File as Urgent if not already, and escalate to Developer for same-day implementation.
- **Add frontend-to-gateway schema validation sync.** The frontend should validate config values against the same Zod schema the gateway uses. Either: (a) export the schema from the agent repo and import in the frontend build, or (b) add a `/validate-config` endpoint that the frontend calls before writing. This prevents the entire class of "agent crash-loops from bad config" bugs.
- **Create a "stuck workspace repair" runbook.** Today's daniel.jindoo fix was ad-hoc. Create a standard procedure: identify crash cause → determine fix → apply remotely or terminate-and-reprovision. Include the common causes (invalid config values, missing gatewayAgentId, nginx down) with their specific fixes. This would make future handoffs to Z faster.
- **Correlate Sentry event flood to boot failures.** If the Sentry flood is indeed driven by the boot failure cascade, fixing the Hetzner rate limit would cut Sentry events by an order of magnitude. Verify this by checking Sentry event timestamps against boot failure windows. If confirmed, this is another reason to prioritize the Hetzner fix.

## Research
- *Skipped — the issues today are internal architecture problems, not externally researchable topics. The fixes are code changes in our own repos.*

## Research reflection
- *N/A — no external research this round.*

## Ideas
- *Covered above — all ideas are based on direct observation of our systems.*

## 🚨 Red Flags
- **Hetzner rate limiting is now a live production outage.** Pool is at 0 ready, 0 booting. New users cannot get workspaces. 22,865 rate limit retries in one check window. The fix (batch API calls) was identified yesterday and is still not implemented. Every hour this persists, new signups fail silently. This needs immediate escalation to the Developer agent and Z/Leon for prioritization. The longer this sits, the more users we lose during onboarding.
- **Sentry at 1.53M/50K events — 30x over quota.** The acceleration is alarming: +385K events in the last 24 hours alone. If this is driven by boot failure spam (likely), fixing the Hetzner rate limit would address both. But if there's a separate Sentry-flooding bug, the billing impact could be significant. Need someone to check the Sentry billing dashboard for on-demand spend.
