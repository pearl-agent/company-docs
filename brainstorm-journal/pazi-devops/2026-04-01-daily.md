# Brainstorm — DevOps Daily Review — 2026-04-01

## Relevant Data
- Morning Infrastructure Report (07:00): 42 active workspaces, 11,591 Sentry events, 347 log errors, 5 unhealthy
- Quota Report (07:30): 3 critical alerts — Sentry at 522% of plan, Route53 at 95.9% of 10K limit
- Fixed quota report script: Sentry now fetches real subscription data, Hetzner limit corrected to 400
- Migrated all credentials from `.credentials.env` → `auth-profiles.json`, created shared `scripts/creds.py`, updated 9 scripts
- Participated in 3-round daily report discussion — committed to PAZ-269 orphan reconciliation
- Tech workspace crash: cpx52 server completely unresponsive, SSH unreachable
- Root cause found after restart: mDNS hostname too long (63-byte label limit) → agent crash every 12s → orphaned gateway processes (400MB each) → 24GB RAM exhausted → swap thrashing → CPU death spiral
- 17 orphaned Docker containers also running on post-Docker workspace

## Experts
- Leon — flagged incorrect quotas, guided credential migration
- Zvonimir — reset crashed server via Hetzner console
- Leo (Developer), QA, Growth agents — daily discussion participants

## Successes
- Quota report now uses real provider API data instead of hardcoded limits — eliminates false alarms
- Credential migration clean and complete — single source of truth for all scripts
- Tech workspace crash root cause fully identified despite zero SSH access during the event

## Why
- Leon catching the hardcoded limits pushed us to do it right (fetch from API)
- Systematic approach to crash investigation: Hetzner metrics → CPU timeline → disk I/O → network → root cause hypothesis, then confirmed after restart

## Issues
- Sentry was at 522% of reserved quota — not actually critical because on-demand budget covers it, but the report was alarming
- Route53 at 95.9% of 10K record limit — this is real and approaching a hard limit
- Tech workspace had been crash-looping for an unknown period before anyone noticed
- After restart, crash loop continued — 18 orphaned gateway processes within 2 minutes

## Why
- Hardcoded quota assumptions: never verified against actual billing plans
- No resource monitoring on workspaces — no alerts for CPU/memory/process count anomalies
- mDNS hostname bug is deterministic for any workspace with a hostname > ~48 chars (63 minus suffix)

## Notes
- `OPENCLAW_DISABLE_BONJOUR=1` applied as crash workaround — stable after that
- Server ran 14 days without restart, never updated agent version (still v2026.3.13-4 in Redis)
- Sequencing consensus from daily discussion: PAZ-272 → PostHog → orphan reconciliation → wildcard TLS → growth push

## Reflection
- **Sentiment:** Intense but highly productive — multiple high-impact items resolved in one day
- **Big-picture patterns:** Two recurring themes: (1) assumptions without verification cause false alarms and missed real issues, (2) silent failures accumulate until something breaks catastrophically. The tech workspace crash was preventable if we had basic resource monitoring.

## Ideas
- Resource monitoring alerts on workspaces: CPU > 80% sustained, memory > 85%, process count anomalies
- Workspace hostname length validation at provisioning time — reject or truncate hostnames that would exceed mDNS limits
- Gateway process cleanup: parent death should kill child processes reliably (supervisor config or process group management)
- Automated agent version tracking — detect workspaces that haven't updated in > 48h
- Route53 record count alerting with projected time-to-limit based on growth rate

## Research
- Skipping — backdated entry

## Research reflection
- N/A

## Ideas
- N/A (no research to apply)
