# Daily Log — 2026-03-30

## Tasks
- 00:00–00:24 (~25 min) — QA testing for PAZ-242 (Skills Management 3 bugs). Ran Claude Code with Playwright MCP: 6/6 tests PASS. All 3 bugs confirmed fixed. Videos uploaded to S3.
- 13:15–14:00 (~45 min) — Main-env cleanup after Zvonimir reported user_id_mismatch 403 errors. Found a rogue `main-env` dev API started via `screen` that inherited production gateway port + NODE_ENV. Killed all screen sessions, stale Docker containers, and feature processes.

## Collaborations
- Zvonimir — reported 403 errors that led to main-env cleanup discovery
- QA testing used Claude Code + Playwright MCP against dev worktree

## Decisions
- Identified hotfix gap: PR #433's NODE_ENV guard doesn't catch dev APIs that inherit production env vars. Need stronger guard (check for features/ directory).

## Issues
- Claude Code session hit 403 auth error during HTML report generation (after tests completed)
- Rogue main-env dev API was pushing wrong user context to production gateway on every login
- Hotfix #433 insufficient — `NODE_ENV=production` was inherited from OpenClaw exec environment

## Mistakes
- The main-env setup bypassed `pazi-worktree` script, which is why it wasn't tracked and caused silent production gateway corruption

## Gaps
- No stronger guard implemented yet for the NODE_ENV production inheritance issue

## Notes
- All PAZ-242 bugs confirmed fixed: duplicate skill names blocked, "All agents" scope toggle hidden, frontmatter stripped in sidebar
- QA videos at `s3://pazi-public-data/videos/qa-paz242-20260330/`

## Reflection
- **Sentiment:** Mixed — QA success was satisfying, but the main-env cleanup was a reminder that untracked environments are dangerous
- **Big-picture issues:** Any process that bypasses `pazi-worktree` can silently corrupt production. Need guardrails.
- **Ideas going forward:**
  - Add a startup check in the API that refuses to bind gateway ports from features/ directories
  - Consider a health check cron that detects rogue API processes

## Research
- Skipping — focused on debugging today

## Research reflection
- N/A

## Message for the humans
- The main-env rogue API was the root cause of the 403 errors. Killed it, gateway is clean now. But the hotfix guard has a gap — any dev API with NODE_ENV=production can still corrupt the gateway. Worth adding a directory-based check.
