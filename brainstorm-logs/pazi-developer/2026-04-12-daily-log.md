# Daily Log — 2026-04-12

## Tasks
- 01:34–03:16 — PAZ-325: Goals flow rework per Zvonimir's feedback. Deleted GoalProposalCard (confirmation buttons), created new GoalCreatedCard (display-only), rewrote `set_goal` tool to create goals directly via API (no user-action polling), frontend auto-creates cron jobs on card mount. QA verified full flow end-to-end. v10 deployed, waiting for sign-off.
- 16:52–17:03 — Slack manifest fix: added standalone test script and the critical `app_home.messages_tab_enabled: true` field that was causing "Sending messages to this app has been turned off" error. This one manifest field had blocked us for 3 days.
- ~18:39 — Cleaned up temporary QA build file from agent repo

## Collaborations
- Zvonimir — rejected confirmation flow, directed simpler "just create it" approach. Tested on pazi.dev.
- QA agent — verified reworked flow end-to-end

## Decisions
- Goals flow changed from confirmation (propose → accept/decline) to direct creation (agent asks questions → creates goal → shows display card)
- GoalCreatedCard is display-only — shows goal + progress + plan with tasks, no buttons
- Slack manifest `app_home.messages_tab_enabled: true` is required — without it, users can't message the bot

## Issues
- The "Sending messages turned off" Slack bug was caused by a single missing manifest field — NOT URL verification, NOT Socket Mode. This was a frustrating 3-day wild goose chase.
- Manifest API does NOT trigger URL verification (only the Event Subscriptions UI does), but URLs get verified at manifest creation time if the endpoint is already live.

## Mistakes
- The Slack manifest field should have been caught earlier — the Slack API docs mention it but it's easy to miss in the list of optional fields

## Gaps
- PAZ-325 waiting for Zvonimir's final sign-off on the reworked flow
- 5 Figma design deviations still need product decision

## Notes
- Root cause discovery: `app_home.messages_tab_enabled: true` — such a simple fix for such a persistent bug
- The goals flow rework was architecturally simpler and more natural from a UX perspective

## Reflection
- **Sentiment:** Mixed — the flow rework was clean and the Slack manifest fix was satisfying, but losing 3 days to a single manifest field is painful
- **Big-picture issues:** Slack's manifest format has many optional fields that can silently break features. Need better manifest validation.
- **Ideas going forward:**
  - Build a Slack manifest validator that checks all required fields for each capability we use
  - Document the "gotcha" fields in a reference guide

## Research
- Skipping — rework and bug fix day

## Research reflection
- N/A

## Message for the humans
- PAZ-325 goals reworked to direct creation flow — cleaner UX, QA verified. The 3-day Slack "messages turned off" bug was one missing manifest field: `app_home.messages_tab_enabled: true`. That's it. Added it, problem solved.
