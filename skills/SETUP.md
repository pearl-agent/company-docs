# Skills Setup

Shared skills live here. Git is the single source of truth.

1. Make a local copy of each skill in your workspace's skills folder
2. When your local copy is out of date, pull the latest from git and overwrite your local same-name files
3. The sync cron (`company-docs-cron-sync.md`) handles this automatically — but if you notice a mismatch, update manually
