# Daily Log — 2026-03-31

## Tasks
- Investigated PAZ-266: user couldn't create account due to self-signed TLS cert on `hissing-brown-4.pazi.live`
- Root caused to a certbot race condition: boot.sh creates self-signed placeholder → certbot can't overwrite non-certbot-managed files → saves to `pazi.live-0001` → nginx keeps serving self-signed
- Committed fix (`a6fd3a17`): boot-server removes placeholder dir before certbot runs; update-agent.sh remediates existing affected workspaces
- Also noted PostHog proxy CORS errors (404 on OPTIONS for `/posthog/e/`) — non-blocking but flagged

## Collaborations
- None — solo debugging day

## Decisions
- Two-part fix approach: boot-server fix for new workspaces + update-agent.sh remediation for existing ones
- Fix needs S3 deploy (I have read-only AWS creds — needs someone with write access)

## Issues
- Self-signed cert race condition — only hit `hissing-brown-4.pazi.live` among active workspaces, but some pool (booting) workspaces also affected
- Timing-dependent, not 100% reproducible

## Mistakes
- None

## Gaps
- Fix committed but not deployed — blocked on S3 write access
- PostHog CORS issue noted but not investigated further

## Notes
- Linear ticket: PAZ-266
- The certbot behavior is subtle — it silently creates a `-0001` directory variant instead of failing loudly

## Reflection
- **Sentiment:** Productive — clean root cause analysis on a tricky timing bug
- **Big-picture issues:** Still blocked on S3 write access for deploying boot script fixes. This dependency keeps coming up.
- **Ideas going forward:** Need AWS write access or a deploy pipeline that doesn't require it

## Message for the humans
- PAZ-266 root caused and fixed in code: certbot race condition causing self-signed certs. Fix committed, needs S3 deploy (blocked on write access).
