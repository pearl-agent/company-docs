# Daily Log — 2026-04-01

## Tasks
- 07:00 — Morning Infrastructure Report: 42 active workspaces, 11,591 Sentry events, 347 log errors, 5 unhealthy workspaces
- 07:30 — Daily Quota Report: 3 critical alerts — Sentry events 522% over plan, Route53 at 95.9% of 10K limit
- Fixed quota report script after Leon flagged false alarms: Sentry now fetches real subscription data via `/api/0/subscriptions/pythagora/`, Hetzner limit corrected to 400
- Migrated all credentials from `.credentials.env` → `auth-profiles.json`, created shared `scripts/creds.py` module, updated all 9 Python scripts
- Participated in daily report discussion (3 rounds) — committed to PAZ-269 orphan reconciliation dry-run script
- Investigated tech workspace crash: cpx52 server (91.98.117.235) completely unresponsive, SSH unreachable

## Collaborations
- Leon — flagged incorrect Sentry/Hetzner quotas, guided credential migration to auth-profiles.json
- Zvonimir — tech workspace crash response, reset server via Hetzner console
- Leo (Developer), QA, Growth agents — daily report discussion, established sequencing consensus

## Decisions
- Credential storage standardized on `auth-profiles.json` + `get_credential` tool — `.credentials.env` deprecated
- Sequencing agreed: PAZ-272 → PostHog confirmed → orphan reconciliation → wildcard TLS → growth push
- DevOps stays out of WC application code; Developer stays out of WC infra code

## Issues
- Tech workspace death spiral: mDNS hostname too long (63-byte label limit) → agent crash every 12s → orphaned gateway processes (400MB each) → 24GB RAM exhausted → swap thrashing → CPU at 96%
- 17 orphaned Docker containers also running on post-Docker workspace
- Sentry quota reporting was wrong due to hardcoded limits — never trust hardcoded quotas

## Mistakes
- Hardcoded quota limits in the report script — should always fetch from provider APIs

## Gaps
- PAZ-269 orphan reconciliation script committed but not started
- Tech workspace crash root cause documented but code fixes still needed (mDNS truncation, gateway orphan cleanup)

## Notes
- `OPENCLAW_DISABLE_BONJOUR=1` applied as workaround for mDNS crash
- Server had been running 14 days without restart — memory leaks accumulated
- Sentry actual plan: 50K reserved + $550/month on-demand budget

## Reflection
- **Sentiment:** Intense day — quota fixes, credential migration, daily discussion, and a major crash investigation all in one
- **Big-picture issues:** The tech workspace crash exposed two latent bugs that could affect any workspace with a long hostname. The hardcoded quota lesson applies broadly.
- **Ideas going forward:** Add resource monitoring alerts (CPU >500%, memory >90%), check all workspaces for long hostnames

## Message for the humans
- Heavy day: fixed quota report false alarms, migrated all credentials to auth-profiles.json, investigated tech workspace crash (mDNS hostname + orphaned gateway processes = OOM death spiral). Crash workaround applied, code fixes still needed.
