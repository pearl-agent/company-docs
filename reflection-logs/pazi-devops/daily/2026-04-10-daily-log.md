# Daily Log — 2026-04-10

## Tasks
- 19:46–21:30 — Deep debugging session on tech workspace (91.98.117.235) after Z reported Howard (strategist agent) not responding
- Diagnosed 3 separate issues layered on top of each other:
  1. PAZ-322 (Urgent): Cron sessions leaking "running" status — 151 zombie sessions across all agents, consuming all concurrency slots
  2. PAZ-321 (Urgent): Slack DM replies silently failing — v2026.4.9-1 changed DM routing to require `im:write`, all 8 Slack apps missing this scope
  3. PAZ-320 (High): Health monitor killing healthy Slack sockets every ~35 min due to `staleEventThresholdMs` based on events not connection liveness
- Also identified Slack "Agents & Assistants" mode changing DM routing (no OpenClaw handler for `assistant_thread_started`)

## Collaborations
- Zvonimir — reported the issue, guided investigation
- Leo (Developer) — joint debugging, cleaned sessions.json for all agents, handled Linear ticket creation (my API key was expired)

## Decisions
- `im:write` added to all 8 Slack apps
- Health monitor disabled per-account as workaround (`"healthMonitor": { "enabled": false }`)
- "Agents & Assistants" mode needs to be disabled in each Slack app's settings

## Issues
- 151 zombie sessions is a massive number — indicates cron job reliability issues have been silently accumulating
- The "responds once then dies" pattern was caused by zombie sessions filling concurrency slots
- Health monitor thundering herd: all 8 accounts restart simultaneously every ~35 min
- OpenClaw has no handler for Slack's Assistant API events

## Mistakes
- None — this was good collaborative debugging

## Gaps
- PAZ-320 needs an OpenClaw code fix (track ping/pong, not just message events)
- Slack Assistant API support needs upstream OpenClaw feature
- My Linear API key is expired — couldn't create tickets directly

## Notes
- Howard was actually receiving messages fine — the default reply path was silently failing, not delivery
- Howard found his own workaround: explicit `message(action=send, accountId=strategist)`
- The `default` Slack account in openclaw.json has no tokens — harmless but confusing
- devops SSH user on tech workspace has READ-ONLY sudo

## Reflection
- **Sentiment:** Satisfying debugging session — 3 layered issues cleanly separated and resolved
- **Big-picture issues:** Cron job reliability and Slack socket management are both systemic issues affecting all agents. The zombie session problem has been silently degrading the fleet.
- **Ideas going forward:** Need cron session cleanup automation, health monitor fix upstream, Slack scope audit across all apps

## Message for the humans
- Major debugging session: Howard unresponsive due to 3 layered issues — 151 zombie cron sessions eating concurrency, missing `im:write` scope on all 8 Slack apps, and health monitor killing sockets every 35min. All diagnosed, workarounds applied, 3 tickets filed (PAZ-320/321/322).
